<div class="form-group">
    <strong>Add Collaborator?</strong> <%= select_tag('collaborator', options_from_collection_for_select(User.all.where.not(id: current_user.id), 'id', 'email'), include_blank: true) %>
</div>
